import * as React from "react";
import { MissingContent } from "../index";

interface Props {}

export const Contact: React.FC<Props> = () => {
    return (
        <div>
            Contact
            <MissingContent />
        </div>
    );
};
